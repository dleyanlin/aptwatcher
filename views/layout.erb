<!DOCTYPE HTML>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
  
    <title>AptWatcher</title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.3/normalize.css">
    <link rel="stylesheet" href="/css/application.css">
  </head>
  
  <body>
    <% if ENV["AUTH_USERNAME"].to_s == "" || ENV["AUTH_PASSWORD"] == "" %>
      <div class="alert">
        <p>You probably want to set the <code>AUTH_USERNAME</code> and <code>AUTH_PASSWORD</code> environment variables to protect this app with HTTP Basic authentication.</p>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
